version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react_router_fe
    ports:
      - "80:3000" 
    environment:
      - VITE_API_URL=http://localhost:8080 
      - NODE_ENV=production
    restart: unless-stopped